#
# This module is provided as propertystore_USE_FILE by propertystoreConfig.cmake.  It can
# be INCLUDEd in a project to load the needed compiler and linker
# settings to use propertystore.
#

IF(NOT propertystore_USE_FILE_INCLUDED)
  SET(propertystore_USE_FILE_INCLUDED 1)

  # Update CMAKE_MODULE_PATH so includes work.
  #SET (CMAKE_MODULE_PATH
  #  ${CMAKE_MODULE_PATH}
  #  ${propertystore_CMAKE_DIR})

  # Add compiler flags needed to use propertystore.
  #SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${propertystore_REQUIRED_C_FLAGS}")
  #SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${propertystore_REQUIRED_CXX_FLAGS}")
  #SET(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${propertystore_REQUIRED_EXE_LINKER_FLAGS}")
  #SET(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} ${propertystore_REQUIRED_SHARED_LINKER_FLAGS}")
  #SET(CMAKE_MODULE_LINKER_FLAGS "${CMAKE_MODULE_LINKER_FLAGS} ${propertystore_REQUIRED_MODULE_LINKER_FLAGS}")

  # Add include directories needed to use propertystore.
  INCLUDE_DIRECTORIES(${propertystore_INCLUDE_DIRS})

ENDIF(NOT propertystore_USE_FILE_INCLUDED)
